export const A_LOOG_NAME_VAR = 0;

export function calc(a: number, b: number) {
  const LOOOOOOOG = "looog";

  return a + b + LOOOOOOOG;
}

export default function demo(a: number, b: number) {
  console.log("demo", a, b);
}
