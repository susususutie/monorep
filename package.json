{
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build-all": "pnpm -r build",
    "build:react": "pnpm -r --filter react-* build"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}